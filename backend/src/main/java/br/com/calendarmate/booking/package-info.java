/**
 * Booking module target boundary.
 *
 * Existing booking endpoints and behavior remain in the legacy controller/service
 * while use cases, policies, DTO mappers, stores, and calendar adapters are
 * extracted incrementally.
 */
package br.com.calendarmate.booking;
